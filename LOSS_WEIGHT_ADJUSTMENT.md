# Loss 权重调整方案

## 修改日期
2026-04-26

## 背景

在红外小目标分割任务中，发现分割效果弱于检测效果。为了平衡两个任务的性能，需要调整分割损失的权重。

## 决策过程

### 问题分析
- 分割（seg）任务表现弱于检测（box）任务
- 需要提高分割损失的权重，增强模型对分割任务的关注度
- 去掉了cls权重，因为是二分类问题

### 方案对比

| 方案 | box | seg | cls | dfl | seg/box |
|------|-----|-----|-----|-----|---------|
| **原配置** | 7.5 | 7.5 | 0.5 | 1.5 | 1.0 |
| 方案 A | 7.5 | 10.0 | 0.0 | 1.5 | 1.33 |
| 方案 B | 7.5 | 12.0 | 0.0 | 1.5 | 1.60 |
| **方案 C (采用)** | 7.5 | 15.0 | 0.0 | 1.5 | 2.00 |
| **方案 D (采用)** | 5.0 | 10.0 | 0.0 | 1.0 | 2.00 |

### 等效性分析

方案 C 和方案 D 的关键比例完全相同：

```
seg/box = 15.0/7.5 = 10.0/5.0 = 2.0
dfl/box = 1.5/7.5 = 1.0/5.0 = 0.2
```

两种方案最终效果等价，方案 D 更简洁整齐。

## 最终配置

### 修改文件
`ultralytics/cfg/default.yaml`

### 配置内容

```yaml
box: 5.0   # (float) box loss gain
seg: 10.0  # (float) segmentation loss gain
cls: 0.0   # (float) classification loss gain
dfl: 1.0   # (float) distribution focal loss gain
```

### 配置说明

| 损失 | 权重 | 相对于 box | 说明 |
|------|------|-----------|------|
| box | 5.0 | 1.0x | 边界框损失，作为基准 |
| seg | 10.0 | **2.0x** | 分割损失，提高至 box 的 2 倍 |
| cls | 0.0 | 0 | 分类损失，禁用（单类场景） |
| dfl | 1.0 | 0.2x | 分布焦点损失，保持低权重 |

### 关键比例

```
seg : box : dfl = 10 : 5 : 1 = 2 : 1 : 0.2
```

## 技术原理

### Loss 归一化机制

YOLO 使用 `nbs` (nominal batch size) = 64 作为基准 batch size：

```
实际梯度更新 = loss × (batch_size / nbs) × 权重
```

例如 batch_size=16 时：
```
最终影响 = loss × (16/64) × 权重 = loss × 0.25 × 权重
```

权重 5.0 和 10.0 本身不是绝对值，而是相对于其他损失的经验比例。

### 为什么 DFL 保持低权重？

| 损失 | 典型量级 | 权重 | 实际影响 |
|------|---------|------|----------|
| box | 0.5-2.0 | 5.0 | 2.5-10.0 |
| seg | 0.3-1.5 | 10.0 | 3.0-15.0 |
| dfl | 0.1-0.5 | 1.0 | 0.1-0.5 |

DFL 的损失量级本身就较小，保持 1.0 权重是合理的。

## 预期效果

1. **分割性能提升**：seg 权重提高 2 倍，模型将更关注分割边界精度
2. **检测性能保持**：box 权重调整后与 seg 保持合理的 1:2 比例
3. **训练稳定性**：cls 禁用避免不必要的梯度干扰
4. **收敛方向**：dfl 权重不变，确保边界定位能力稳定

## 后续调优建议

如果发现训练效果不佳，可以按以下方向微调：

| 方向 | box | seg | dfl | 说明 |
|------|-----|-----|-----|------|
| 更关注分割 | 5.0 | 12.0 | 1.0 | seg 权重提高 20% |
| 更关注检测 | 6.0 | 10.0 | 1.2 | box/dfl 同比例提高 |
| 平衡模式 | 5.0 | 8.0 | 1.0 | seg 降低，训练初期使用 |
