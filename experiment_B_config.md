# 实验B: ConvNeXt-Seg 红外小目标分割训练配置

## \subsection{软硬件环境与超参数}

### 硬件环境
- **GPU**: NVIDIA TITAN Xp × 1 (12GB 显存)
- **CPU**: 多核处理器
- **内存**: 充足的系统内存

### 软件环境
- **深度学习框架**: PyTorch + Ultralytics YOLO11
- **CUDA 版本**: 12.4
- **训练策略**: 两阶段训练 (冻结 + 解冻)

### 训练超参数

| 参数 | 阶段1 (冻结) | 阶段2 (微调) |
|------|-------------|-------------|
| Epochs | 30 | 90 |
| Batch Size | 24 | 24 |
| 图像尺寸 | 640×640 | 640×640 |
| 优化器 | AdamW | AdamW |
| 初始学习率 (lr0) | 0.001 | 0.0005 |
| 最终学习率 (lrf) | 0.1 | 0.1 |
| 余弦退火 | 是 | 是 |
| 冻结层数 | 5 | 0 (全部解冻) |
| Weight Decay | 0.05 | 0.05 |
| Warmup Epochs | 3.0 | 3.0 |
| Warmup Momentum | 0.8 | 0.8 |
| Warmup Bias LR | 0.1 | 0.1 |
| Workers | 8 | 8 |
| AMP 混合精度 | 是 | 是 |

---

## \subsection{损失函数权重}

训练脚本中配置的损失函数权重如下，这些权重在计算总损失时被应用到对应的损失项上：

| 损失类型 | 参数名 | 权重值 | 说明 |
|----------|--------|--------|------|
| 边界框损失 | `box` | 7.5 | 边界框回归损失 |
| 分割损失 | `seg` | 15.0 | 实例分割掩膜损失 |
| 分类损失 | `cls` | 1.0 | 目标类别分类损失 |
| DFL损失 | `dfl` | 3.0 | Distribution Focal Loss |

**总损失计算**：

```
total_loss = box_loss × 7.5 + seg_loss × 15.0 + cls_loss × 1.0 + dfl_loss × 3.0
```

**权重配置来源**：这些权重在 `train_convnext_seg_B.py` 的 `config` 字典中定义（第415-419行）：

```python
# Loss 权重配置
"box": 7.5,
"cls": 1.0,
"dfl": 3.0,
"seg": 15.0,
```

---

## \subsection{数据集}

### 数据集来源

本实验使用的红外小目标检测（Infrared Small-Target Detection, IRSTD）数据集由以下三个公开数据集整合而成：

| 数据集 | 原始格式 | 特点 |
|--------|----------|------|
| SIRST\_V2 | 图像 + xml bbox + png mask | 原有xml标注不直接使用，统一从mask重新生成bbox |
| NUDT\_SIRST | 图像 + mask | 无官方划分，需自行划分 |
| IRSTD\_1k | 图像 + mask | 有官方train/val/test划分 |

数据整理流程遵循严格的检查规范：

1. **统一目录结构与命名规则**：建立stage1\_cleaned统一目录，图像统一重命名为`{数据集前缀}_{序号}.png`
2. **图像与mask配对检查**：验证每张图是否有同名mask、文件可读性、尺寸一致性
3. **mask质量可视化抽检**：每个数据集至少抽取20张样本，覆盖正常前景、空mask、异常样本三类
4. **bbox统一由mask重新生成**：保证三套数据bbox口径一致
5. **最终转换为COCO格式**：生成统一annotations

### 数据集规格

- **数据集规模（整合后）**:
  - 训练集: 2,347 张图片
  - 验证集: 504 张图片
  - 测试集: 425 张图片
- **图像尺寸**: 640×640 像素（统一裁剪为正方形）
- **输入通道**: 1（灰度/红外单通道）
- **图像格式**: PNG
- **mask格式**: 二值图（背景=0，前景=255）
- **标注格式**: YOLO分割格式 / COCO格式

### 图像预处理

由于原始红外图像多为长方形（如640×512、256×256等），在统一裁剪为640×640正方形时，采用**无关灰度值填充**策略：

- **填充方式**: 将非目标区域填充为固定灰度值（如0或均值灰度），避免引入偏置
- **裁剪策略**: 优先保留图像中心区域，或采用滑动窗口覆盖全图
- **mask处理**: 填充区域的mask统一为背景（值为0），保证前景目标区域不受影响

该策略确保：
1. 模型输入尺寸统一，便于batch训练
2. 填充区域不携带语义信息，避免干扰模型学习
3. mask与图像严格对齐，前景区域完整保留

### 数据增强方法

| 增强方法 | 参数值 | 说明 |
|----------|--------|------|
| Mosaic | 1.0 | 开启，概率100% |
| HSV (H) | 0.015 | 色相变换 |
| HSV (S) | 0.7 | 饱和度变换 |
| HSV (V) | 0.4 | 亮度变换 |
| 水平翻转 | 0.5 | 概率50% |
| 垂直翻转 | 0.0 | 关闭 |
| 平移 | 0.1 | 10%平移 |
| 缩放 | 0.5 | 50%尺度变换 |
| Mixup | 0.0 | 关闭 |
| Copy-paste | 0.0 | 关闭 |

### 分割任务特定参数
- **Overlap Mask**: True (启用)
- **Mask Ratio**: 4.0 (下采样比例) ✓ 最终确定

---

## \subsection{预训练权重}

- **模型架构**: ConvNeXt-Base
- **预训练数据集**: ImageNet-22k (大规模图像分类数据集)
- **微调数据集**: ImageNet-1k (1000类图像分类)
- **权重文件**: `convnext_base_in22k_ft_in1k.pth`
- **权重加载策略**:
  - 阶段1: 加载 ImageNet-22k 预训练权重到 ConvNeXt backbone
  - 阶段2: 从阶段1最佳权重继续训练，全部参数解冻

---

## \subsection{评测指标}

### 训练过程指标 (CSV日志)

| 指标名称 | 说明 |
|----------|------|
| train/box_loss | 边界框回归损失 |
| train/seg_loss | 分割损失 |
| train/cls_loss | 分类损失 |
| train/dfl_loss | Distribution Focal Loss |
| val/box_loss | 验证集边界框损失 |
| val/seg_loss | 验证集分割损失 |
| lr/pg0, pg1, pg2 | 各参数组学习率 |

### 主要评测指标

**最终配置**: Mosaic开启 + Mask Ratio=4.0 + 两阶段训练(freeze+finetune)

该配置在分割任务上表现最优。

| 指标 | 说明 | 最终结果 (Epoch 60) |
|------|------|---------------------|
| Precision (B) | 边界框精确率 | 0.829 |
| Recall (B) | 边界框召回率 | 0.711 |
| mAP@50 (B) | IoU=0.5 时边界框 mAP | 0.733 |
| mAP@50-95 (B) | IoU 0.5-0.95 平均 mAP | 0.338 |
| Precision (M) | 掩膜精确率 | 0.656 |
| Recall (M) | 掩膜召回率 | 0.569 |
| mAP@50 (M) | IoU=0.5 时掩膜 mAP | 0.536 |
| mAP@50-95 (M) | IoU 0.5-0.95 掩膜 mAP | 0.225 |

### 评估设置
- **IoU Threshold**: 0.5
- **验证频率**: 每个 epoch 结束后验证
- **最佳模型选择**: 基于验证集 mAP@50 最高

---

## \subsection{模型架构改进}

实验B在 Baseline 基础上增加了 **通道适配器 (Channel Adapter)**:

1. **问题**: 红外图像是单通道输入，而 ImageNet 预训练模型期望3通道RGB输入
2. **解决方案**: 在模型最前端添加 1→3 通道适配器层
3. **初始化**: 使用 ImageNet 权重均值初始化适配器
4. **可学习**: 适配器参数可训练，允许微调适应红外特征
