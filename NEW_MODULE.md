# 红外小目标联合感知——特征增强模块总结

## 引言

本文档对论文《基于大规模视觉模型的红外小目标联合分割与检测研究》中涉及的四个特征增强模块进行系统总结，内容涵盖各模块的理论依据、模型结构、期望解决的问题、预期效果及添加位置，以供 Codex 进行代码搭建与实验。

---

## 一、空间到深度变换模块 (Space-to-Depth, SPD)

### 理论依据

传统下采样方法（如跨步卷积或池化操作）遵循奈奎斯特采样定律。当空间采样频率低于红外小目标的高频特征时，会产生混叠效应，导致图像中的高频细节信息丢失。SPD 变换通过将空间信息无损地重新排列到通道维度，在降低分辨率的同时保留了全部信息。

### 模型结构

对于输入特征图 $X \in \mathbb{R}^{S \times S \times C}$，其运算过程如下：

1. 按缩放因子（文中取 $\text{scale}=2$）将空间像素按固定步长拆分为子特征图 $f_{x,y}$：

$$
f_{x,y} = X[x:S:\text{scale},\; y:S:\text{scale}]
$$

其中 $x,y \in \{0, \ldots, \text{scale}-1\}$。以 $\text{scale}=2$ 为例，将产生四个子图 $f_{0,0}, f_{1,0}, f_{0,1}, f_{1,1}$。

2. 将上述子图沿通道维度进行拼接，得到输出特征图：

$$
\mathrm{SPD}(X) = \mathrm{Concat}(f_{0,0}, f_{0,1}, f_{1,0}, f_{1,1}) \in \mathbb{R}^{\frac{S}{2} \times \frac{S}{2} \times 4C}
$$

### 期望解决的问题

红外小目标在下采样过程中的信号消失或响应衰减问题。

### 预期效果

实现特征表达的"无损编码"。变换前后特征张量的元素总量保持恒定，确保代表微弱目标的每一个像素分量都能完整流向深层网络。在大幅降低空间分辨率的同时，依然能捕获并区分真实弱小目标与孤立噪声点。

### 添加位置

替换模型中的传统下采样层（跨步卷积或池化层）。

---

## 二、频域特征提取模块 (Frequency-Domain Feature Extraction)

### 理论依据

红外图像可以被视作不同空间频率分量的叠加。大面积的背景干扰（如云层、地表热辐射）主要占据低频分量，而具有高亮、孤立特征的小目标则主要贡献于高频分量。离散傅里叶变换（DFT）能将特征从空间域映射至频域，实现对全局信息的跨维度建模。根据卷积定理，空间域的复杂卷积运算可以等价地转化为频域内的逐点乘法，通过构造可学习的频域滤波器，能够实现对特定频率信号的选择性过滤。

### 模型结构

该模块的运算流程如下：

1. **频域变换：** 对输入特征图 $f(x,y)$ 应用二维离散傅里叶变换，得到频域表示 $F(u,v)$：

$$
F(u,v) = \sum_{x=0}^{M-1} \sum_{y=0}^{N-1} f(x,y) e^{-j2\pi\left(\frac{ux}{M} + \frac{vy}{N}\right)}
$$

2. **频域滤波：** 利用可学习的频域响应函数 $H(u,v)$ 对频谱进行逐点乘法，实现自适应滤波：

$$
G(u,v) = F(u,v) \cdot H(u,v)
$$

3. **空间域还原：** 对增强后的频域信号 $G(u,v)$ 进行逆离散傅里叶变换，将特征还原回空间域：

$$
g(x,y) = \frac{1}{MN} \sum_{u=0}^{M-1} \sum_{v=0}^{N-1} G(u,v) e^{j2\pi\left(\frac{ux}{M} + \frac{vy}{N}\right)}
$$

### 期望解决的问题

空间域卷积感受野有限，难以在早期阶段进行全局背景感知，导致模型难以利用目标与背景在频域上的能量分布差异。

### 预期效果

在特征流转初期建立全局联系，自适应地对低频背景分量进行抑制，并对代表小目标的高频信号进行增益。从能量分布维度解耦目标与背景，有效辅助大规模视觉模型在极低信噪比环境下区分真实弱小目标与孤立噪声点，降低后续检测与分割分支的处理难度。

### 添加位置

主干网络特征提取的早期阶段。

---

## 三、快速空间金字塔池化注意力模块 (Fast Spatial Pyramid Pooling Attention, FSPPA)

### 理论依据

受空间金字塔池化（Spatial Pyramid Pooling）思想和红外小目标非对称上下文调制研究的启发。红外小目标因探测距离变化，在图像中呈现不同尺度。通过在不同感受野下整合目标周边的环境信息，模型能够学习到适应目标大小的注意力权重。

### 模型结构

该模块的运算过程可抽象为两个步骤：

1. **多尺度特征提取：** 利用不同范围因子的池化操作获取多分辨率感受野，并进行跨尺度融合：

$$
F_{ms} = \Psi(P_k(F))
$$

其中，$F$ 为输入特征图，$P_k$ 表示在不同范围因子 $k$ 下进行的信息提取，$\Psi$ 代表跨尺度融合算子。

2. **注意力增强：** 基于汇总的多尺度信息生成注意力权重，并与原始输入特征逐元素相乘：

$$
F_{out} = F \otimes \Phi(F_{ms})
$$

其中，$\Phi$ 将汇总后的多尺度信息转化为注意力权重，$\otimes$ 表示逐元素相乘。

### 期望解决的问题

目标在不同成像距离下呈现出的尺度波动性，导致单一感受野的模型难以稳健感知所有目标。

### 预期效果

在单一层级内整合并利用多尺度上下文信息，生成能自动适应目标大小的注意力权重，确保模型对不同尺寸的红外小目标均具备稳健的感知能力。

### 添加位置

主干网络的特征输出端。

---

## 四、级联通道空间卷积注意力模块 (Cascaded Channel-Spatial Convolutional Attention, CASCA)

### 理论依据

基于卷积块注意力模块（CBAM）的研究，将通道注意力和空间注意力以"先通道、后空间"的顺序级联使用，能够取得优于单一维度增强的效果。其内在逻辑在于：通道注意力先行对语义通道进行筛选，抑制无益的背景纹理通道，为后续的空间注意力提供信噪比更高的特征基底；空间注意力仅需在已精炼的特征空间上定位目标位置，从而避免了无效通道干扰导致的权重分配偏差。

### 模型结构

设输入中间特征图为 $F$，模块的计算流程如下：

1. **通道注意力加权：** 利用通道注意力映射 $M_c$ 生成加权后的特征 $F_c$：

$$
F_c = M_c(F) \otimes F
$$

2. **空间注意力加权：** 基于已优化的特征分布，利用空间注意力映射 $M_s$ 进一步锁定像素级位置，得到最终输出 $F'$：

$$
F' = M_s(F_c) \otimes F_c
$$

其中 $\otimes$ 表示逐元素相乘。该模块以级联方式实现先通道筛选、后空间定位的深度特征精炼。

### 期望解决的问题

极低信噪比场景下，无效背景通道的干扰会严重影响空间定位的精度，导致目标响应强度不足。

### 预期效果

显著增强极小目标的响应强度，使模型在抑制大范围非均匀背景杂波时具备更强的鲁棒性。通过对特征响应的自适应调节，确保检测与分割解码器在复杂背景下实现更精确的定位和掩膜生成。

### 添加位置

检测与分割的解码器（任务分支头）中。

---

## 总结

| 模块名称 | 核心机制 | 添加位置 | 主要作用 |
|---------|---------|---------|---------|
| SPD 变换 | 空间到深度无损重组 | 替换传统下采样层 | 防止小目标信息在下采样中丢失 |
| 频域特征提取 | DFT 变换 + 可学习频域滤波 | 主干网络早期阶段 | 全局背景感知，频域解耦目标与背景 |
| FSPPA | 多尺度金字塔池化 + 注意力 | 主干网络输出端 | 适应目标尺度变化，多尺度稳健感知 |
| CASCA | 级联通道-空间注意力 | 检测/分割解码器 | 深度特征精炼，增强目标响应强度 |