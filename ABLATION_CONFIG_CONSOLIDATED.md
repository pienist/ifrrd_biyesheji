# ConvNeXt-Seg 红外小目标分割 - 消融实验配置

> **文档版本**: v7.0 | **更新于**: 2026-04-26 | **状态**: 损失函数编号已按优先级重新排列（L1=最高优先）

---

## 1. 实验配置总览

### 实验策略：两阶段消融

```
┌─────────────────────────────────────────────────────────────────┐
│                        实验总体策略                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  第一阶段：损失函数对比（基于实验B）                               │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  B (Baseline) → 对比多种损失函数 → 选出最佳损失函数      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                          │                                      │
│                          ▼                                      │
│  第二阶段：模块消融实验（基于最佳损失函数）                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Best Loss → +SPD → +频域模块 → +FSPPA → +CASCA         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 设计原则

| 原则 | 说明 |
|------|------|
| 公平性 | 所有实验使用相同的训练策略，只改变模型/损失 |
| 必要性 | 红外小目标数据少(4k)，必须两阶段训练 |
| 效率性 | 消融实验需要多次运行，配置要合理 |
| 可比性 | 冻结/解冻配置保持一致，只改变模块 |

---

## 2. 实验结果汇总

### 已完成实验

| 实验 | 方案 | mAP50(M) | mAP50-95(M) | 状态 |
|------|------|:--------:|:------------:|:-----:|
| **A** | Baseline | **0.4561** | **0.1748** | ✅ |
| **B** | Channel Adapter (BCEDice) | **0.4597** | **0.1907** | ✅ |

---

## 3. 第一阶段：损失函数对比实验

### 实验设计

> **目标**: 在实验B的基础上，对比多种损失函数，选出最佳方案
>
> **注意**: 检测和分割任务采用端到端联合训练，所有损失函数都会回传到主干网络（Backbone）进行梯度更新

```
B (Baseline: BCEDice) ─┬─ L1:  SLSIoU Loss (位置敏感+形状感知)
                       ├─ L2:  Tversky Loss (α=0.3, β=0.7, 减少漏检)
                       ├─ L3:  FocalDice Loss (难例+小目标)
                       ├─ L4:  SoftIoU Loss (小目标友好)
                       ├─ L5:  WeightedBCE+Dice (正类加权)
                       ├─ L6:  BCE + Dice + Focal (前景+难样本组合)
                       ├─ L7:  BCE + Edge + Dice (边界+全局组合)
                       ├─ L8:  BCE + Focal Loss (难样本挖掘)
                       ├─ L9:  BCE + Dice Loss (不依赖像素数量)
                       ├─ L10: BCE + Texture Loss (纹理一致性)
                       ├─ L11: BCE + Edge Loss (边界感知)
                       ├─ L12: BCE + IoU Loss (补充全局度量)
                       └─ L13: Combo Loss (包含BCE, 不推荐)
```

### 损失函数分类与说明

#### A. YOLO 原生分割损失 (Baseline)

| 实验 | 损失函数 | 权重配置 | 联合主干 | 红外小目标适用性 |
|------|----------|----------|:--------:|-----------------|
| **B** | BCEDiceLoss (50%BCE + 50%Dice) | seg=10.0 | ✅ | ⭐⭐⭐ (基准) |

#### B. 最高优先级损失函数 (L1-L3)

> **原因**: 这些损失函数专门为红外小目标设计，支持位置敏感、漏检减少、难例聚焦等特性

| 优先级 | 实验 | 损失函数 | 核心作用 | 联合主干 | 推荐理由 |
|:------:|------|----------|----------|:--------:|----------|
| 🥇 1 | **L1** | **SLSIoU Loss** | 位置敏感IoU | ✅ | 位置+形状敏感，warmup机制，专门为小目标设计 |
| 🥇 2 | **L2** | **Tversky Loss** | FN权重可调 | ✅ | 增大FN权重(β=0.7)，专门减少小目标漏检 |
| 🥇 3 | **L3** | **FocalDice Loss** | Dice+Focal组合 | ✅ | 难例聚焦(γ=2.0)+小目标友好 |

#### C. 高优先级损失函数 (L4-L6)

| 优先级 | 实验 | 损失函数 | 核心作用 | 联合主干 | 推荐理由 |
|:------:|------|----------|----------|:--------:|----------|
| 🥈 4 | **L4** | **SoftIoU Loss** | 带权重的IoU | ✅ | 小目标友好，支持class_weight=[1.0, 50.0] |
| 🥈 5 | **L5** | **WeightedBCE+Dice** | 加权BCE+Dice | ✅ | 正类加权(pos_weight=10.0)，增强前景学习 |
| 🥈 6 | **L6** | **BCE + Dice + Focal** | 前景+难样本组合 | ✅ | 前景增强+难样本挖掘 |

#### D. 中优先级损失函数 (L7-L9)

| 优先级 | 实验 | 损失函数 | 核心作用 | 联合主干 | 红外小目标适用性 |
|:------:|------|----------|----------|:--------:|-----------------|
| 🥉 7 | **L7** | **BCE + Edge + Dice** | 边界+全局组合 | ✅ | ⭐⭐⭐⭐ |
| 🥉 8 | **L8** | BCE + Focal Loss | 难样本挖掘 | ✅ | ⭐⭐⭐⭐ |
| 🥉 9 | **L9** | BCE + Dice Loss | 类别不平衡友好 | ✅ | ⭐⭐⭐⭐ |

#### E. 低优先级损失函数 (L10-L12)

| 优先级 | 实验 | 损失函数 | 核心作用 | 联合主干 | 红外小目标适用性 |
|:------:|------|----------|----------|:--------:|-----------------|
| - 10 | **L10** | BCE + Texture Loss | 纹理一致性 | ✅ | ⭐⭐⭐ |
| - 11 | **L11** | BCE + Edge Loss | 边界感知 | ✅ | ⭐⭐⭐ |
| - 12 | **L12** | BCE + IoU Loss | 补充全局度量 | ✅ | ⭐⭐⭐ |

#### F. 不推荐使用的损失函数

| 实验 | 损失函数 | 不推荐原因 |
|------|----------|------------|
| L13 | **Combo Loss** | 已包含BCE，继承了BCELoss的潜在问题 |

### 损失函数原理详解

---

#### L1: SLSIoU Loss (Scale and Location Sensitive IoU)

**核心原理**

SLSIoU (Scale and Location Sensitive IoU) Loss来自MSHNet，包含IoU损失和位置敏感损失(LLoss)。

```
数学公式:

IoU = |P ∩ T| / |P ∪ T|

α = (min(P,T) + dis) / (max(P,T) + dis)
SIoU = α × IoU

LLoss = (1 - Length_Loss) + Angle_Loss

总损失:
L_total = 1 - SIoU + LLoss (warmup后)

其中:
- dis = (P_sum - T_sum)² / 4  (距离因子)
- Length_Loss: 长度损失
- Angle_Loss: 角度损失
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 位置敏感 | 考虑预测和GT的中心位置偏差 |
| 形状敏感 | LLoss关注目标形状 |
| Warmup机制 | 前几个epoch只用IoU，后面加入LLoss |

---

#### L2: Tversky Loss

**核心原理**

Tversky Loss是Dice Loss的泛化形式，通过引入α和β参数分别控制假阳性(FP)和假阴性(FN)的权重。

```
数学公式:

Tversky = TP / (TP + α·FP + β·FN)

Tversky Loss = 1 - Tversky

参数说明:
- α: FP(误报)的权重
- β: FN(漏检)的权重
- 对于小目标，FN比FP更重要，所以设置 β > α
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 可调FN权重 | 小目标漏检代价高，增大β减少FN |
| 适应不平衡 | 通过α/β平衡FP和FN的惩罚 |
| 灵活控制 | 可根据任务需求调整检测策略 |

---

#### L3: FocalDice Loss

**核心原理**

FocalDice Loss结合了Dice Loss和Focal Loss的思想，对难例（低Dice值）给予更高权重。

```
数学公式:

Dice = 2|P∩T| / (|P| + |T|)

Focal Weight = (1 - Dice)^γ

FocalDice Loss = Focal_Weight × (1 - Dice)

其中 γ 通常取2，γ越大，对难例的聚焦越强
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 难例聚焦 | 对低IoU/Dice的样本给予更高权重 |
| 小目标友好 | 难分割的小目标获得更多关注 |
| 自适应权重 | 无需手动设置，根据Dice自动调整 |

---

#### L4: SoftIoU Loss

**核心原理**

SoftIoU Loss直接优化IoU，同时支持类别权重来处理小目标问题。

```
数学公式:

IoU = |P ∩ T| / |P ∪ T|

SoftIoU Loss = 1 - IoU

带class_weight版本:
- 前景（目标）赋予更高权重
- 前景权重 = fg_weight, 背景权重 = bg_weight
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 直接优化IoU | 与评估指标一致 |
| 支持类别权重 | 前景加权增强小目标学习 |
| 数值稳定 | 带smooth项防止除零 |

---

#### L5: WeightedBCE+Dice Loss

**核心原理**

加权BCE + Dice Loss，对正类（目标）赋予更高权重来平衡类别不均。

```
数学公式:

L_BCE = -pos_weight × GT × log(P) - (1-GT) × log(1-P)
L_Dice = 1 - (2×|P∩GT| + ε) / (|P| + |GT| + ε)

L_total = w_bce × L_BCE + w_dice × L_Dice

参数:
- pos_weight: 正类权重 (如10.0)
- 更高的正类权重使模型更关注小目标
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 正类加权 | 解决前景占比极小的问题 |
| 双重优化 | BCE提供像素级监督，Dice提供全局度量 |
| 灵活调参 | pos_weight可根据数据调整 |

---

#### L6: BCE + Dice + Focal (组合)

**核心原理**

结合前景增强（Dice）和难样本聚焦（Focal），特别适合极端不平衡的小目标分割。

```
总损失:
L_total = L_BCE + λ_dice * L_dice + λ_focal * L_focal

各分量:
- L_BCE: 像素级分类基础损失
- L_dice: Dice损失（前景增强）
- L_focal: Focal损失（难样本聚焦）
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 极端不平衡 | 小目标前景占比极小，Dice+Focal组合有效 |
| 难样本挖掘 | Focal Loss聚焦于难检测的小目标 |
| 鲁棒性强 | 对噪声和低对比度更鲁棒 |

---

#### L7: BCE + Edge + Dice (组合)

**核心原理**

结合边界优化（Edge）和全局度量（Dice），同时提升边界精度和整体分割质量。

```
总损失:
L_total = L_BCE + λ_edge * L_edge + λ_dice * L_dice

各分量:
- L_BCE: 像素级分类基础损失
- L_edge: 边缘感知损失（边界精度）
- L_dice: Dice损失（全局重叠度量）
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 双重优化 | 同时优化边界和整体，质量更高 |
| 互补性强 | Edge优化局部，Dice优化全局 |
| 适用性广 | 对各种尺度的目标都有效 |

---

#### L8: Focal Loss

**核心原理**

Focal Loss来自RetinaNet，通过降低易分类样本的权重，聚焦于难分类样本。

```
数学公式:

FL(p_t) = -α_t * (1 - p_t)^γ * log(p_t)

其中:
- p_t: 真实类别的预测概率
- α_t: 类别权重（前景/背景平衡）
- γ (gamma): 聚焦参数，通常取2

二分类简化形式:
Focal Loss = -[(1-p)^γ * log(p)] (正样本)
            -[p^γ * log(1-p)]   (负样本)
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 难样本聚焦 | 红外小目标往往难以检测，Focal Loss聚焦于难样本 |
| 类别平衡 | 自动平衡前景与背景的权重 |
| γ控制强度 | γ越大，对易样本的抑制越强 |

---

#### L9: Dice Loss

**核心原理**

Dice系数源于医学图像分割，与IoU类似但对前景（目标）更敏感。

```
数学公式:

Dice = 2|P ∩ T| / (|P| + |T|)

Dice Loss = 1 - Dice

概率版本:
Dice Loss = 1 - (2 * Σ(p_i * t_i) + ε) / (Σ(p_i) + Σ(t_i) + ε)
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 前景敏感 | Dice系数对前景像素更敏感，适合小目标 |
| 类别不平衡友好 | 前景占比很小时，Dice仍能有效优化 |
| 收敛快 | 对正样本的梯度更大，收敛速度更快 |

---

#### L10: Texture Loss (纹理感知损失)

**核心原理**

纹理损失通过分块统计量（均值、方差）匹配，保留局部纹理一致性。

```
数学公式:

纹理统计量:
μ_p = AvgPool(P)        # 局部均值
σ²_p = AvgPool(P²) - μ_p²  # 局部方差

纹理损失:
L_texture = ||μ_p - μ_t||₁ + ||σ²_p - σ²_t||₁

总损失: L_total = L_BCE + λ_texture * L_texture
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 局部一致性 | 红外图像纹理微弱，纹理损失保持局部统计一致性 |
| 抗噪声 | 分块操作平滑噪声影响 |
| 亮度保持 | 避免预测结果整体过亮或过暗 |

---

#### L11: Edge Loss (边缘感知损失)

**核心原理**

边缘损失在BCE基础上添加边缘检测损失，引导模型关注目标边界。

```
数学公式:

Edge Loss = L1(|∇P| - |∇T|)

Sobel算子:
Gx = [-1, 0, 1]    Gy = [-1, -2, -1]
     [-2, 0, 2]         [ 0,  0,  0]
     [-1, 0, 1]         [ 1,  2,  1]

|∇P| = sqrt(Gx(P)² + Gy(P)²)

总损失: L_total = L_BCE + λ_edge * L_edge
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 边缘优化 | 红外小目标边界模糊，边缘损失提升边界精度 |
| 噪声抵抗 | 噪声通常无规则边缘，边缘损失可区分 |
| 定位精度 | 直接优化边界质量，提升mAP50-95 |

---

#### L12: IoU Loss (Intersection over Union)

**核心原理**

IoU Loss 直接优化预测掩膜与真实掩膜的交并比，这是分割任务最直接的评估指标。

```
数学公式:

IoU = |P ∩ T| / |P ∪ T|

IoU Loss = 1 - IoU = 1 - |P ∩ T| / |P ∪ T|

其中:
- P: 预测掩膜
- T: 真实掩膜
- ∩: 交集运算
- ∪: 并集运算
```

**为什么适用于红外小目标？**

| 特性 | 说明 |
|------|------|
| 全局度量 | IoU考虑整体重叠，不受单像素错误影响 |
| 尺度不变 | 对小目标和大人目标的敏感度一致 |
| 与评估指标一致 | mAP基于IoU计算，直接优化可提升评估指标 |

---

#### L13: Combo Loss (不推荐)

**核心原理**

Combo Loss是BCE和Dice Loss的线性组合，兼顾像素级和全局优化。

```
数学公式:

Combo Loss = α × L_BCE + β × L_Dice

其中:
- L_BCE: 标准BCE损失
- L_Dice: 1 - Dice系数
- α + β = 1 (通常 α=0.5, β=0.5)
```

**为什么不推荐？**

| 特性 | 说明 |
|------|------|
| 包含BCE | 继承了BCELoss的潜在问题 |
| 效果不确定 | 在极端不平衡场景下可能不如纯Dice系列 |

---

### 实验配置

```yaml
# ========== 基础配置（所有损失函数实验共用）==========
backbone: "convnext_base_in22k_ft_in1k.pth"
freeze_layers: 5
stage1: { epochs: 30, lr0: 0.001, lrf: 0.1 }
stage2: { epochs: 90, lr0: 0.0005, lrf: 0.1 }
augmentation: { hsv_h: 0.015, hsv_s: 0.7, hsv_v: 0.4, flipud: 0.0, fliplr: 0.5, mosaic: 1.0 }
YOLO配置: box=5.0, seg=10.0, cls=0.0, dfl=1.0

# ========== Baseline: B (BCEDiceLoss, 已完成) ==========
experiment_name: "B_channel_adapter"
loss: { type: "bce_dice", bce_weight: 0.5, dice_weight: 0.5 }
结果: mAP50=0.4597, mAP50-95=0.1907 ✅

# ==================== 最高优先级 (L1-L3) ====================
# mtl_loss.py 损失函数 - 专门为红外小目标设计

# 🥇 L1: SLSIoU Loss (优先级1)
experiment_name: "L1_slsiou"
loss: { type: "slsiou", warm_epoch: 5, class_weight: [1.0, 50.0] }
推荐理由: 位置敏感+形状感知，warmup机制，专门为小目标设计

# 🥇 L2: Tversky Loss (优先级2)
experiment_name: "L2_tversky"
loss: { type: "tversky", alpha: 0.3, beta: 0.7 }
推荐理由: 增大FN权重(β=0.7)，专门减少小目标漏检

# 🥇 L3: FocalDice Loss (优先级3)
experiment_name: "L3_focal_dice"
loss: { type: "focal_dice", gamma: 2.0 }
推荐理由: 难例聚焦(γ=2.0)+小目标友好

# ==================== 高优先级 (L4-L6) ====================

# 🥈 L4: SoftIoU Loss (优先级4)
experiment_name: "L4_softiou"
loss: { type: "softiou", class_weight: [1.0, 50.0] }
推荐理由: 小目标友好，支持前景加权

# 🥈 L5: WeightedBCE+Dice (优先级5)
experiment_name: "L5_weighted_bce_dice"
loss: { type: "weighted_bce_dice", pos_weight: 10.0 }
推荐理由: 正类加权，增强前景学习信号

# 🥈 L6: BCE + Dice + Focal (优先级6)
experiment_name: "L6_bce_dice_focal"
loss: { type: "bce_dice_focal", bce_weight: 1.0, dice_weight: 0.5, focal_weight: 0.5, gamma: 2.0 }
推荐理由: 前景增强+难样本挖掘

# ==================== 中优先级 (L7-L9) ====================

# 🥉 L7: BCE + Edge + Dice (优先级7)
experiment_name: "L7_bce_edge_dice"
loss: { type: "bce_edge_dice", bce_weight: 1.0, edge_weight: 0.1, dice_weight: 0.5 }
推荐理由: 边界+全局双重优化

# 🥉 L8: BCE + Focal Loss (优先级8)
experiment_name: "L8_bce_focal"
loss: { type: "bce_focal", bce_weight: 1.0, focal_weight: 0.5, gamma: 2.0 }

# 🥉 L9: BCE + Dice Loss (优先级9)
experiment_name: "L9_bce_dice"
loss: { type: "bce_dice", bce_weight: 1.0, dice_weight: 0.5 }

# ==================== 低优先级 (L10-L12) ====================

# L10: BCE + Texture Loss
experiment_name: "L10_bce_texture"
loss: { type: "bce_texture", bce_weight: 1.0, texture_weight: 0.05 }

# L11: BCE + Edge Loss
experiment_name: "L11_bce_edge"
loss: { type: "bce_edge", bce_weight: 1.0, edge_weight: 0.1 }

# L12: BCE + IoU Loss
experiment_name: "L12_bce_iou"
loss: { type: "bce_iou", bce_weight: 1.0, iou_weight: 0.5 }

# ==================== 不推荐 (L13) ====================

# ❌ L13: Combo Loss (包含BCE，效果不确定)
experiment_name: "L13_combo"
loss: { type: "combo", alpha: 0.5, beta: 0.5 }
注意: 包含BCE，继承潜在问题，效果可能不如纯Dice系列
```

### 第一阶段预期结果

#### 已完成实验

| 实验 | 方案 | mAP50(M) | mAP50-95(M) | 状态 |
|------|------|:--------:|:------------:|:-----:|
| **B** | BCEDice (Baseline) | **0.4597** | **0.1907** | ✅ |

#### 待执行实验 (按优先级排序)

| 优先级 | 实验 | 方案 | 核心特性 | 预期提升 | 状态 |
|:------:|------|------|----------|:--------:|:-----:|
| 🥇 1 | L1 | **SLSIoU Loss** | 位置敏感+形状感知 | 高 | ⏳ |
| 🥇 2 | L2 | **Tversky Loss** | 减少漏检 (β=0.7) | 高 | ⏳ |
| 🥇 3 | L3 | **FocalDice Loss** | 难例聚焦 (γ=2.0) | 高 | ⏳ |
| 🥈 4 | L4 | **SoftIoU Loss** | 小目标友好 | 中高 | ⏳ |
| 🥈 5 | L5 | **WeightedBCE+Dice** | 正类加权 | 中高 | ⏳ |
| 🥈 6 | L6 | **BCE + Dice + Focal** | 前景+难样本组合 | 中高 | ⏳ |
| 🥉 7 | L7 | BCE + Edge + Dice | 边界+全局组合 | 中 | ⏳ |
| 🥉 8 | L8 | BCE + Focal Loss | 难样本挖掘 | 中 | ⏳ |
| 🥉 9 | L9 | BCE + Dice Loss | 类别不平衡友好 | 中 | ⏳ |
| - 10 | L10 | BCE + Texture Loss | 纹理一致性 | 待定 | ⏳ |
| - 11 | L11 | BCE + Edge Loss | 边界感知 | 待定 | ⏳ |
| - 12 | L12 | BCE + IoU Loss | 补充全局度量 | 待定 | ⏳ |
| ❌ | L13 | Combo Loss | 包含BCE | 不确定 | ⏳ |

**选择标准**: 选 **mAP50-95 最高** 的作为"最佳损失函数基线"

---

## 4. 第二阶段：特征增强模块消融实验

### 模块概述

| 模块名称 | 核心机制 | 添加位置 | 主要作用 |
|---------|---------|---------|---------|
| **SPD** | Space-to-Depth 无损下采样 | 替换传统下采样层 | 防止小目标信息在下采样中丢失 |
| **频域模块** | DFT变换 + 可学习频域滤波 | 主干网络早期阶段 | 全局背景感知，频域解耦目标与背景 |
| **FSPPA** | 多尺度金字塔池化 + 注意力 | 主干网络输出端 | 适应目标尺度变化，多尺度稳健感知 |
| **CASCA** | 级联通道-空间注意力 | 检测/分割解码器 | 深度特征精炼，增强目标响应强度 |

### 实验设计

> **前提**: 基于第一阶段选出的最佳损失函数（假设 L6 效果最好）

```
Best Loss (基线)
      │
      ├── M1: +SPD
      │         │
      │         └── M2: +SPD + 频域模块
      │                   │
      │                   └── M3: +SPD + 频域 + FSPPA
      │                             │
      │                             └── M4: +SPD + 频域 + FSPPA + CASCA (最终版)
      │
      └── 单独验证每个模块的贡献
```

### 模块详情

#### M1: SPD 空间到深度变换

**说明**:
- **目的**: 传统下采样会导致小目标信息丢失
- **原理**: 将空间信息无损重组到通道维度
- **效果**: 保留全部高频细节，增强弱小目标检测

```yaml
# 实验M1: SPD变换
experiment_name: "M1_spd"
new_module: ["spd"]  # 替换传统下采样层
```

#### M2: 频域特征提取模块

**说明**:
- **目的**: 空间卷积感受野有限，难以全局感知
- **原理**: DFT变换 + 可学习频域滤波，自适应过滤高低频
- **效果**: 从频域维度解耦目标与背景

```yaml
# 实验M2: SPD + 频域模块
experiment_name: "M2_spd_freq"
new_module: ["spd", "frequency_domain"]
```

#### M3: FSPPA 多尺度注意力

**说明**:
- **目的**: 目标尺度变化，单一感受野难以适应
- **原理**: 多尺度金字塔池化 + 跨尺度注意力融合
- **效果**: 自动适应不同尺寸目标，增强多尺度感知

```yaml
# 实验M3: SPD + 频域 + FSPPA
experiment_name: "M3_spd_freq_fsppa"
new_module: ["spd", "frequency_domain", "fsppa"]
```

#### M4: CASCA 级联注意力

**说明**:
- **目的**: 极低信噪比下目标响应不足
- **原理**: 先通道筛选（抑制背景通道），后空间定位
- **效果**: 增强目标响应，抑制背景杂波

```yaml
# 实验M4: 完整版
experiment_name: "M4_full"
new_module: ["spd", "frequency_domain", "fsppa", "casca"]
```

### 第二阶段预期结果

| 实验 | 方案 | mAP50(M) | mAP50-95(M) | 状态 |
|------|------|:--------:|:------------:|:-----:|
| M1 | +SPD | 待做 | 待做 | ⏳ |
| M2 | +SPD + 频域 | 待做 | 待做 | ⏳ |
| M3 | +SPD + 频域 + FSPPA | 待做 | 待做 | ⏳ |
| M4 | +SPD + 频域 + FSPPA + CASCA | 待做 | 待做 | ⏳ |

---

## 5. 训练策略

### 两阶段训练配置（所有实验共用）

| 阶段 | Epochs | 学习率 | 策略 | 训练部分 |
|------|--------|--------|------|----------|
| **阶段1 (冻结)** | 30 | 1e-3 → 1e-4 | cosine | Neck + Head + 新增模块 |
| **阶段2 (微调)** | 90 | 5e-4 → 5e-5 | cosine | 全部参数 |

### Epoch配置建议

| 数据集大小 | 阶段1 | 阶段2 | 总计 |
|-----------|-------|-------|------|
| < 2k | 50 | 150 | 200 |
| 2k - 5k | **30** | **90** | **120** |
| 5k - 10k | 20 | 80 | 100 |
| > 10k | 10 | 50 | 60 |

### 学习率调整建议

| 情况 | 调整建议 |
|------|----------|
| 损失不收敛 | 提高lr0 1.5-2倍 |
| 损失震荡 | 降低lr0 0.5倍 |
| 过拟合 | 降低lr0，增加weight_decay |
| 欠拟合 | 提高lr0，增加warmup |

---

## 6. 执行顺序建议

### 推荐批次与优先级

> **重要**: 按照实验优先级分批次执行，编号顺序即执行顺序

| 批次 | 实验 | 优先级 | 核心目标 | 说明 |
|------|------|:------:|----------|------|
| ✅ 批次0 | A, B | - | 建立基准 | 已完成 |
| 🔴 **批次1** | **L1, L2, L3** | 🥇 | mtl_loss.py 核心损失 | SLSIoU + Tversky + FocalDice |
| 🟡 **批次2** | **L4, L5, L6** | 🥈 | mtl_loss.py 补充 + 组合 | SoftIoU + WeightedBCE + BCE+Dice+Focal |
| 🟡 **批次3** | **L7, L8, L9** | 🥉 | BCE组合系列 | Edge+Dice, Focal, Dice |
| 🟢 **批次4** | L10, L11, L12 | - | 辅助损失 | Texture, Edge, IoU |
| ❌ **批次5** | L13 | ❌ | Combo Loss | 包含BCE，不推荐 |

### 执行流程图

```
批次1 (最高优先级 - 3个核心损失函数):
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  L1 SLSIoU ──────────> 位置敏感+形状感知 ──> 基准对比    │
│  L2 Tversky ─────────> 减少小目标漏检 ────> 漏检优先      │
│  L3 FocalDice ────────> 难例聚焦 ─────────> 综合提升      │
│                                                              │
│  选出最佳: mAP50-95 最高的作为下一阶段基线               │
└─────────────────────────────────────────────────────────────┘

批次2 (高优先级 - 补充验证):
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  L4 SoftIoU ───────────> 小目标友好 + 前景加权             │
│  L5 WeightedBCE ───────> 正类加权增强                     │
│  L6 BCE+Dice+Focal ────> 前景+难样本组合                  │
│                                                              │
│  补充验证: 与批次1结果交叉对比                              │
└─────────────────────────────────────────────────────────────┘

批次3 (中优先级 - 辅助验证):
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  L7 BCE+Edge+Dice ────> 边界+全局双重优化                 │
│  L8 BCE+Focal ─────────> 难样本挖掘                        │
│  L9 BCE+Dice ──────────> 类别不平衡友好                    │
│                                                              │
│  验证: BCE+Dice组合系列的额外增益                          │
└─────────────────────────────────────────────────────────────┘
```

### 详细执行顺序

```
第一阶段: 损失函数对比 (共4个批次)

批次1 (最高): 核心 mtl_loss.py 损失
────────────────────────────────────────────────
B (Baseline) → L1(SLSIoU) → L2(Tversky) → L3(FocalDice)
              │
              ▼ 选出最佳 (假设 L1 最好)
              └── L1 作为"最佳损失函数基线"

批次2 (高): 补充 mtl_loss.py + 组合损失
────────────────────────────────────────────────
B → L4(SoftIoU) → L5(WeightedBCE) → L6(BCE+Dice+Focal)
              │
              ▼ 与批次1结果对比
              └── 确认最佳方案

批次3 (中): BCE+Dice 组合系列
────────────────────────────────────────────────
B → L7(Edge+Dice) → L8(Focal) → L9(Dice)

批次4 (低): 辅助损失函数
────────────────────────────────────────────────
B → L10(Texture) → L11(Edge) → L12(IoU)

第二阶段: 特征增强模块 (基于最佳损失函数)
────────────────────────────────────────────────
Best Loss → M1(SPD) → M2(SPD+频域) → M3(SPD+频域+FSPPA) → M4(完整版)
```

---

## 7. 配置检查清单

```
□ 预训练权重: convnext_base_in22k_ft_in1k.pth
□ 数据集配置: dataset.yaml / dataset_B.yaml
□ 硬件资源: GPU显存 >= 16GB (batch=8)
□ 模型配置: ultralytics/cfg/models/convnext/convnext-seg.yaml
□ 损失函数 (L1-L7): BCE, IoU, Dice, Focal, Edge, Texture (待实现)
□ 损失函数 (L8-L13): Tversky, SoftIoU, FocalDice, Combo, SLSIoU, WeightedBCE (mtl_loss.py)
□ 特征模块: SPD, 频域, FSPPA, CASCA (待实现)
```

---

## 8. 最终配置汇总

| 参数 | 值 | 说明 |
|------|-----|------|
| 冻结Epochs | 30 | 训练Neck+Head+新增模块 |
| 解冻Epochs | 90 | 整体微调 |
| 总Epochs | 120 | 30 + 90 |
| 冻结学习率 | 1e-3 → 1e-4 | cosine衰减 |
| 解冻学习率 | 5e-4 → 5e-5 | cosine衰减 |
| Batch Size | 8 | 16GB显存 |
| 冻结层数 | 5 | 前5层 |

---

## 9. 实验预期提升路径

```
第一阶段：损失函数优化
────────────────────────────────────────────────────────
B (Baseline)                    0.4597 mAP50
      │
      ├── +Edge Loss     →  ~0.47 mAP50  (+2%)
      ├── +Dice Loss     →  ~0.47 mAP50  (+2%)
      └── +Edge + Dice   →  ~0.48 mAP50  (+4%)

第二阶段：特征增强模块
────────────────────────────────────────────────────────
Best Loss                       ~0.48 mAP50
      │
      ├── +SPD           →  ~0.50 mAP50  (+4%)
      ├── +频域模块       →  ~0.51 mAP50  (+2%)
      ├── +FSPPA         →  ~0.52 mAP50  (+2%)
      └── +CASCA         →  ~0.54 mAP50  (+4%)

最终目标: 0.54+ mAP50
```

---

*配置生成时间: 2026-04-19*
*最后更新: 2026-04-26 (v7.0 损失函数编号按优先级重新排列：L1=最高优先)*
