# ConvNeXt-Base 红外小目标分割消融实验方案

## 红外小目标特性分析

### 1. 红外图像与自然图像的本质差异

| 特性 | 自然图像(RGB) | 红外图像 |
|------|---------------|----------|
| **成像原理** | 可见光反射 | 红外热辐射 |
| **通道数** | 3通道(RGB) | 1通道(灰度) |
| **信息载体** | 颜色+形状+纹理 | **仅亮度/温度** |
| **纹理特征** | 丰富清晰 | 模糊微弱 |
| **边缘特征** | 锐利分明 | 模糊渐变 |
| **噪声特性** | 相对稳定 | 显著且复杂 |

### 2. 红外小目标的独特挑战

```
┌─────────────────────────────────────────────────────────┐
│                    红外小目标特性                         │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ① 目标尺寸极小                                           │
│     - 通常 < 32×32 像素                                   │
│     - 在640×640图像中占比 < 0.25%                          │
│     - 有效特征信息极其稀少                                 │
│                                                          │
│  ② 形状纹理不清晰                                          │
│     - 红外成像分辨率较低                                   │
│     - 目标边界模糊、渐变                                   │
│     - 缺乏锐利的边缘和纹理                                 │
│                                                          │
│  ③ 微弱局部亮度异常                                        │
│     - 小目标可能仅表现为局部"热点"                         │
│     - 与背景对比度差异微小                                 │
│     - 没有完整的几何形状                                   │
│                                                          │
│  ④ 易被噪声淹没                                            │
│     - 热噪声、量化噪声、系统噪声                            │
│     - 弱小目标信号强度接近噪声水平                          │
│     - 信噪比(SNR)极低                                     │
│                                                          │
│  ⑤ 1通道输入 vs 3通道主干                                 │
│     - ImageNet预训练权重基于3通道                          │
│     - 直接适配存在通道不匹配问题                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 3. 核心问题

ConvNeXt-Base在ImageNet上预训练时：
- **输入**: 3通道RGB图像
- **学习**: 颜色+形状+纹理+边缘的混合特征
- **权重**: 针对3通道优化

应用到红外1通道任务时：
- **输入**: 1通道灰度图像
- **问题**: 通道不匹配 + 颜色特征完全无用 + 纹理模式差异

---

## 消融实验方案总览

| 实验 | 方案 | 针对特性 | 优先级 |
|------|------|----------|--------|
| **A** | Baseline (无改进) | 基准对比 | 必须 |
| **B** | 通道适配器 (1→3卷积) | ① 通道不匹配 | ⭐⭐⭐⭐⭐ |
| **C** | CLAHE红外增强 | ② ③ 对比度增强 | ⭐⭐⭐⭐⭐ |
| **D** | 边缘感知损失 | ② 形状边界 | ⭐⭐⭐⭐ |
| **E** | 纹理感知损失 | ② 纹理模式 | ⭐⭐⭐ |
| **F** | B+C组合 | 通道+对比度 | ⭐⭐⭐⭐⭐ |
| **G** | B+C+D组合 | 通道+对比度+边缘 | ⭐⭐⭐⭐⭐ |
| **H** | B+C+D+E组合 (全量) | 全部改进 | ⭐⭐⭐⭐⭐ |

---

## 方案详解

---

### 实验A: Baseline (基准)

**配置**: ConvNeXt-Base + 原始训练配置，无任何改进

**目的**: 
- 提供基准性能对比
- 验证其他改进的有效性

**预期结果**: mAP50(M) ≈ 0.44-0.50 (基于历史实验)

---

### 实验B: 通道适配器 (Channel Adapter)

#### 核心原理

```
ImageNet预训练权重 (3通道):
┌────────────────────────────────────────┐
│  Conv2d(3, 96, kernel=4×4, stride=4)   │
│                                        │
│  权重形状: [96, 3, 4, 4]                │
│  └─────────────────────────────────┘   │
│           3通道输入                      │
└────────────────────────────────────────┘

红外输入图像 (1通道):
┌────────────────────────────────────────┐
│  灰度图像 [H, W]                        │
│                                        │
│  像素值 = 温度/亮度强度                  │
└────────────────────────────────────────┘

问题: 如何用1通道输入适配3通道权重？
```

#### 解决方案: 1×1卷积投影

```python
class ChannelAdapter(nn.Module):
    """1通道 → 3通道适配器"""
    
    def __init__(self, pretrained_weight):
        super().__init__()
        
        # 1×1卷积: 1通道 → 3通道
        # 不改变空间尺寸，只改变通道数
        self.proj = nn.Conv2d(1, 3, kernel_size=1, bias=False)
        
        # 权重初始化: 使用ImageNet 3通道权重的均值
        self._init_from_imagenet(pretrained_weight)
    
    def _init_from_imagenet(self, weight_3ch):
        """
        初始化原理:
        
        ImageNet权重对3个通道是独立学习的:
        weight_3ch[:, 0, :, :]  → R通道权重
        weight_3ch[:, 1, :, :]  → G通道权重  
        weight_3ch[:, 2, :, :]  → B通道权重
        
        对于灰度图像，R=G=B，所以我们取3个通道的均值:
        weight_1ch = mean(weight_3ch[:, [0,1,2], :, :], dim=1)
        
        这样初始化保证:
        1. 权重的"感受野模式"得以保留
        2. 3通道的信息被压缩到1通道
        3. 模型从合理的初始化开始学习
        """
        # weight_3ch: [C_out, 3, H, W]
        weight_mean = weight_3ch.mean(dim=1, keepdim=True)  # [C_out, 1, H, W]
        self.proj.weight.data = weight_mean
        
        # 可选: 偏置初始化
        self.proj.bias.data = weight_3ch.mean(dim=(1, 2, 3))
    
    def forward(self, x):
        # x: [B, 1, H, W] 红外单通道
        # 输出: [B, 3, H, W] 3通道 (适配ImageNet权重)
        return self.proj(x)
```

#### 为什么有效?

| 方面 | 解释 |
|------|------|
| **通道转换** | 1×1卷积是最轻量的通道转换方式，不引入额外参数 |
| **保留预训练知识** | 使用均值初始化，保留了ImageNet学到的"空间模式" |
| **可学习微调** | 卷积层权重可学习，允许模型适应红外特性 |
| **计算效率** | 1×1卷积计算量极小，几乎不影响推理速度 |

#### 数学解释

```
原始ImageNet前向:
y = Conv3×3(W_3ch, x_3ch) = Σᵢ W_3ch[:, i] * x_3ch[:, i]

红外适配前向:
y = Conv1×1(W_1to3, x_1ch) = W_1to3 * x_1ch
  = mean(W_3ch, dim=1) * x_1ch  (初始化时)
  ≈ Σᵢ (W_3ch[:, i] / 3) * x_1ch
  ≈ (W_R + W_G + W_B) / 3 * x_gray

物理意义: 红外亮度 ≈ (R + G + B) / 3 = 灰度值
```

---

### 实验C: CLAHE红外增强

#### 核心原理

**CLAHE (Contrast Limited Adaptive Histogram Equalization)** 是一种自适应直方图均衡化方法。

##### 问题: 红外图像对比度低

```
原始红外图像直方图:
┌──────────────────────────────────────┐
│                                      │
│          ████                         │
│        ████████                       │
│      ████████████                     │
│    ████████████████                   │
│  ████████████████████████             │
├──────────────────────────────────────┤
│ 0    亮度值(0-255)                 255│
│                                      │
│ 分布集中，对比度差，小目标淹没在背景中  │
└──────────────────────────────────────┘

CLAHE增强后直方图:
┌──────────────────────────────────────┐
│      █                               │
│    █ █     █                         │
│  █ █ █ █ █ █ █                       │
│  █ █ █ █ █ █ █ █ █                   │
│  █ █ █ █ █ █ █ █ █ █ █ █             │
├──────────────────────────────────────┤
│ 0    亮度值(0-255)                 255│
│                                      │
│ 分布均匀，对比度增强，细节更明显       │
└──────────────────────────────────────┘
```

##### CLAHE vs 普通直方图均衡化

| 方法 | 原理 | 红外效果 |
|------|------|----------|
| **普通HE** | 全局直方图均衡 | ❌ 过度增强噪声，放大伪影 |
| **CLAHE** | 分块直方图+对比度限制 | ✅ 增强对比度同时抑制噪声 |

##### CLAHE算法原理

```python
def apply_clahe(img, clip_limit=2.0, tile_grid_size=(8, 8)):
    """
    CLAHE 步骤:
    
    1. 分块 (Tiling)
       将图像划分为8×8个小块，每块独立处理
    
    2. 局部直方图均衡
       每个小块内进行直方图均衡化
    
    3. 对比度限制 (Clip Limit)
       限制直方图高度，防止过度增强噪声
       - 统计每个bin的像素数
       - 如果超过 clip_limit * total / n_bins，裁剪
       - 将裁剪的部分均匀分配给其他bin
    
    4. 双线性插值
       块边界处使用双线性插值，避免接缝
    """
    
    # 转为uint8格式 (CLAHE需要)
    img_uint8 = (img * 255).astype(np.uint8)
    
    # 创建CLAHE对象
    clahe = cv2.createCLAHE(clipLimit=clip_limit, tileGridSize=tile_grid_size)
    
    # 应用CLAHE
    img_clahe = clahe.apply(img_uint8)
    
    return img_clahe.astype(np.float32) / 255.0
```

##### 为什么对红外小目标特别有效?

```
┌─────────────────────────────────────────────────────────┐
│                   CLAHE对红外小目标的作用               │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ① 增强局部对比度                                        │
│     - 小目标与背景的亮度差异被放大                       │
│     - 原本淹没在背景中的微弱热点变得可见                  │
│                                                          │
│  ② 抑制全局噪声                                          │
│     - 对比度限制防止噪声被过度放大                       │
│     - 弱小目标的信号被增强，噪声被相对压制               │
│                                                          │
│  ③ 保留边缘和形状                                        │
│     - 自适应处理不会模糊边缘                             │
│     - 分块处理保持局部结构                               │
│                                                          │
│  ④ 提升信噪比                                            │
│     - 背景噪声被抑制                                     │
│     - 目标信号被增强                                     │
│     - SNR显著提升                                       │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

##### 参数选择

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| `clip_limit` | 2.0-4.0 | 越小噪声抑制越强，越大对比度越强 |
| `tile_grid_size` | (8,8) | 越小局部适应性越好，但可能产生块效应 |

---

### 实验D: 边缘感知损失 (Edge-Aware Loss)

#### 核心原理

红外小目标的形状往往不完整，边界模糊，传统的BCE损失只关注像素分类，不关注边缘质量。

##### 问题: BCE损失的缺陷

```
BCE Loss = -Σ [y·log(ŷ) + (1-y)·log(1-ŷ)]

BCE问题:
1. 对所有像素一视同仁
2. 不考虑空间相关性
3. 对边缘模糊不敏感
4. 可能产生模糊的预测边界

例如:
真实目标:  ████████████
预测1:     ██████████░░  (BCE低，但边缘模糊)
预测2:     ████░░░░░████  (BCE相同，但形状错误)

BCE无法区分这两种情况！
```

##### 解决方案: 边缘感知损失

```python
class EdgeAwareLoss(nn.Module):
    """
    边缘感知损失 = BCE Loss + Edge Loss
    
    Edge Loss: 使用Sobel算子检测边缘，计算预测边缘与真实边缘的差异
    """
    
    def __init__(self, edge_weight=0.1):
        super().__init__()
        self.edge_weight = edge_weight
        
        # Sobel算子 - 边缘检测经典方法
        # Sobel X: 检测水平边缘
        # Sobel Y: 检测垂直边缘
        sobel_x = torch.tensor([
            [-1, 0, 1],
            [-2, 0, 2],
            [-1, 0, 1]
        ], dtype=torch.float32)
        
        sobel_y = torch.tensor([
            [-1, -2, -1],
            [ 0,  0,  0],
            [ 1,  2,  1]
        ], dtype=torch.float32)
        
        # 注册为buffer (不参与梯度计算，但会随模型移动)
        self.register_buffer('sobel_x', sobel_x.unsqueeze(0).unsqueeze(0))
        self.register_buffer('sobel_y', sobel_y.unsqueeze(0).unsqueeze(0))
    
    def compute_edges(self, x):
        """
        计算图像边缘强度
        
        Sobel算子原理:
        Gx = [-1 0 1]    Gy = [-1 -2 -1]
             [-2 0 2]         [ 0  0  0]
             [-1 0 1]         [ 1  2  1]
        
        G = sqrt(Gx² + Gy²)
        
        对于图像中的边缘:
        - 灰度值突变 → Gx, Gy大 → G大 → 边缘
        - 灰度值平滑 → Gx, Gy小 → G小 → 平坦区域
        """
        # 计算水平梯度
        edge_x = F.conv2d(x, self.sobel_x, padding=1)
        # 计算垂直梯度
        edge_y = F.conv2d(x, self.sobel_y, padding=1)
        # 合成边缘强度
        edge = torch.sqrt(edge_x**2 + edge_y**2 + 1e-6)
        
        return edge
    
    def forward(self, pred, target):
        # 标准BCE损失
        loss_bce = F.binary_cross_entropy(pred, target)
        
        # 边缘损失
        pred_edge = self.compute_edges(pred)
        target_edge = self.compute_edges(target)
        loss_edge = F.l1_loss(pred_edge, target_edge)
        
        # 组合损失
        return loss_bce + self.edge_weight * loss_edge
```

##### 为什么边缘损失对红外小目标有效?

```
┌─────────────────────────────────────────────────────────┐
│               边缘损失对红外小目标的作用                 │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  红外小目标的边缘特征:                                    │
│                                                          │
│  ① 边缘是目标存在的关键线索                              │
│     - 虽然形状模糊，但边缘仍然存在                         │
│     - 边缘损失鼓励模型学习精确的边界定位                   │
│                                                          │
│  ② 抑制边缘模糊                                          │
│     - BCE可能产生"过度平滑"的边界                         │
│     - 边缘损失惩罚边界模糊的预测                          │
│                                                          │
│  ③ 提升小目标定位精度                                    │
│     - 小目标边缘通常更关键                               │
│     - 边缘损失直接优化边界质量                           │
│                                                          │
│  ④ 抵抗背景噪声干扰                                      │
│     - 噪声通常没有明显的边缘结构                          │
│     - 边缘损失帮助模型聚焦于目标边界                       │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

##### 边缘检测的可视化理解

```
原始图像:        Sobel边缘检测:
┌──────────┐      ┌──────────┐
│          │      │  ████    │
│  ██████  │  →   │ ██████   │
│  ██████  │      │ ██████   │
│          │      │  ████    │
└──────────┘      └──────────┘
目标区域         检测到的边缘线

边缘损失的作用:
- 让预测的边缘线尽量接近真实边缘线
- 边界更清晰，定位更准确
```

---

### 实验E: 纹理感知损失 (Texture-Aware Loss)

#### 核心原理

红外图像虽然纹理模糊，但局部区域的"纹理模式"（亮度分布的统计特性）仍然是有价值的信息。

##### 问题: 红外纹理的特点

```
自然图像纹理:              红外图像纹理:
┌──────────┐              ┌──────────┐
│ ▓▓░░▓▓░░ │              │ ░░░░░░░░ │
│ ░░▓▓░░▓▓ │              │ ░░▒▒░░░░ │
│ ▓▓░░▓▓░░ │              │ ░▒▒▒▒░░░ │
│          │              │ ░░▒▒░░░░ │
└──────────┘              └──────────┘
丰富清晰                  模糊微弱

红外纹理的统计特性:
- 局部均值 (亮度级别)
- 局部方差 (对比度)
- 局部直方图分布
```

##### 解决方案: 纹理感知损失

```python
class TextureAwareLoss(nn.Module):
    """
    纹理感知损失 = BCE Loss + Texture Loss
    
    Texture Loss: 使用分块方差匹配，学习局部纹理模式
    """
    
    def __init__(self, texture_weight=0.05, patch_size=16):
        super().__init__()
        self.texture_weight = texture_weight
        self.patch_size = patch_size  # 分块大小
    
    def compute_texture_stats(self, x):
        """
        计算局部纹理统计量
        
        原理:
        - 局部均值 μ: 反映局部亮度
        - 局部方差 σ²: 反映局部对比度/纹理强度
        
        对于每个patch:
        μ = (1/N) Σᵢⱼ x[i,j]
        σ² = (1/N) Σᵢⱼ (x[i,j] - μ)²
        """
        # 全局平均池化到 patch_size × patch_size
        # 输入: [B, 1, H, W]
        # 输出: [B, 1, patch_size, patch_size]
        pooled = F.avg_pool2d(
            x, 
            kernel_size=self.patch_size,
            stride=self.patch_size
        )
        
        # 计算局部均值 (就是avg_pool2d的结果)
        local_mean = pooled
        
        # 计算局部方差
        # Var[X] = E[X²] - E[X]²
        pooled_sq = F.avg_pool2d(
            x ** 2,
            kernel_size=self.patch_size,
            stride=self.patch_size
        )
        local_var = pooled_sq - local_mean ** 2
        
        return local_mean, local_var
    
    def forward(self, pred, target):
        # 标准BCE损失
        loss_bce = F.binary_cross_entropy(pred, target)
        
        # 纹理损失
        pred_mean, pred_var = self.compute_texture_stats(pred)
        target_mean, target_var = self.compute_texture_stats(target)
        
        # 均值差异 (亮度级别)
        loss_mean = F.mse_loss(pred_mean, target_mean)
        
        # 方差差异 (对比度/纹理强度)
        loss_var = F.mse_loss(pred_var, target_var)
        
        # 纹理损失 = 均值损失 + 方差损失
        loss_texture = loss_mean + loss_var
        
        return loss_bce + self.texture_weight * loss_texture
```

##### 为什么纹理损失对红外小目标有效?

```
┌─────────────────────────────────────────────────────────┐
│              纹理损失对红外小目标的作用                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  红外小目标的纹理信息:                                    │
│                                                          │
│  ① 局部亮度分布                                          │
│     - 小目标通常是局部"亮点"或"暗点"                      │
│     - 纹理损失学习正确的局部均值分布                       │
│                                                          │
│  ② 局部对比度模式                                        │
│     - 目标与背景的对比度是识别关键                        │
│     - 方差损失保持正确的对比度级别                        │
│                                                          │
│  ③ 抵抗均匀背景                                          │
│     - 红外背景往往是相对均匀的                           │
│     - 纹理损失帮助区分"均匀背景"和"纹理目标"               │
│                                                          │
│  ④ 多尺度感知                                            │
│     - 不同patch_size感知不同尺度的纹理                    │
│     - 16×16适合中等目标，8×8适合微小目标                  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

##### 纹理损失的可视化理解

```
图像分块示意:
┌──────────────────────────────────────┐
│ ┌────────┬────────┬────────┬──────┐ │
│ │ Patch1 │ Patch2 │ Patch3 │ ...  │ │
│ ├────────┼────────┼────────┼──────┤ │
│ │ Patch5 │ Patch6 │ Patch7 │ ...  │ │
│ ├────────┼────────┼────────┼──────┤ │
│ │  ...   │  ...   │  ...   │      │ │
│ └────────┴────────┴────────┴──────┘ │
└──────────────────────────────────────┘

每个Patch计算:
- 均值 μ: 局部平均亮度
- 方差 σ²: 局部对比度/纹理强度

纹理损失要求:
- pred_patch的μ接近target_patch的μ
- pred_patch的σ²接近target_patch的σ²
```

---

### 实验F: 通道适配器 + CLAHE (B+C)

#### 组合原理

```
单独B (通道适配):
- ✓ 解决通道不匹配
- ✗ 对比度问题未解决

单独C (CLAHE):
- ✓ 增强对比度
- ✗ 通道不匹配未解决

B+C组合:
- ✓ 同时解决通道和对比度问题
- ✓ 协同增强效果
```

#### 实现

```python
class InfraredOptimizedPipeline:
    """
    红外优化Pipeline
    1. CLAHE增强 (数据预处理)
    2. 1→3通道投影 (模型输入适配)
    """
    
    def __init__(self, clip_limit=2.0, tile_size=(8, 8)):
        self.clahe = cv2.createCLAHE(clipLimit=clip_limit, tileGridSize=tile_size)
        self.channel_adapter = None  # 在模型中定义
    
    def preprocess(self, img):
        """数据预处理"""
        # img: [1, H, W] tensor
        
        # Step 1: CLAHE增强
        img_np = (img.squeeze().cpu().numpy() * 255).astype(np.uint8)
        img_clahe = self.clahe.apply(img_np)
        img_clahe = torch.from_numpy(img_clahe).float() / 255.0
        
        return img_clahe.unsqueeze(0)  # [1, H, W]
    
    def forward(self, x):
        """模型前向"""
        # Step 2: 1→3通道投影
        x = self.channel_adapter(x)  # [B,1,H,W] → [B,3,H,W]
        # 后续主干网络处理...
        return output
```

---

### 实验G: 通道适配器 + CLAHE + 边缘损失 (B+C+D)

#### 组合原理

```
B+C: 解决输入适配和对比度问题

+ D (边缘损失): 
- 在特征层面优化边缘质量
- 与CLAHE协同: CLAHE增强边缘对比度 → 边缘损失优化边缘定位
- 双重边缘增强效果
```

#### 协同效应

```
CLAHE效果:
- 增强目标与背景的边缘对比度
- 使边缘更明显、更易于检测

边缘损失效果:
- 惩罚预测边缘与真实边缘的偏差
- 使模型学会精确的边缘定位

协同效果:
- CLAHE让边缘"可见"
- 边缘损失让边缘"精准"
- 1 + 1 > 2
```

---

### 实验H: 全量组合 (B+C+D+E)

#### 最终方案

```
B: 通道适配器 (1→3)
C: CLAHE增强
D: 边缘感知损失
E: 纹理感知损失
```

#### 综合效果

```
┌─────────────────────────────────────────────────────────┐
│                   全量方案综合效果                         │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  输入处理:                                                │
│  ┌─────────────┐    ┌─────────────┐                    │
│  │  红外1通道   │ → │  CLAHE增强   │ → [1,H,W]          │
│  └─────────────┘    └─────────────┘                    │
│         ↓                                               │
│  ┌─────────────┐                                        │
│  │ 1→3通道适配  │ → [3,H,W] (适配ImageNet权重)           │
│  └─────────────┘                                        │
│                                                          │
│  损失函数:                                                │
│  ┌─────────────┐    ┌─────────────┐                    │
│  │   BCE Loss  │ +  │ 边缘损失    │ + 纹理损失          │
│  │  (基础分类) │    │ (边缘精准)  │    (纹理模式)        │
│  └─────────────┘    └─────────────┘                    │
│                                                          │
│  最终效果:                                                │
│  - 输入适配 ✓                                            │
│  - 对比度增强 ✓                                          │
│  - 边缘精准 ✓                                            │
│  - 纹理学习 ✓                                            │
│  - 全面优化 ✓                                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 实验配置汇总

### 实验列表

| 实验 | 通道适配 | CLAHE | 边缘损失 | 纹理损失 | 预期mAP50(M) |
|------|:--------:|:-----:|:--------:|:--------:|:-------------:|
| A | ✗ | ✗ | ✗ | ✗ | ~0.44-0.50 |
| B | ✓ | ✗ | ✗ | ✗ | ~0.46-0.52 |
| C | ✗ | ✓ | ✗ | ✗ | ~0.48-0.54 |
| D | ✗ | ✗ | ✓ | ✗ | ~0.45-0.51 |
| E | ✗ | ✗ | ✗ | ✓ | ~0.44-0.50 |
| F | ✓ | ✓ | ✗ | ✗ | ~0.50-0.56 |
| G | ✓ | ✓ | ✓ | ✗ | ~0.53-0.58 |
| H | ✓ | ✓ | ✓ | ✓ | ~0.56-0.62 |

### 参数配置建议

```yaml
# 通道适配器
channel_adapter:
  init_method: "imagenet_mean"  # 使用ImageNet均值初始化
  learnable: true  # 可学习微调

# CLAHE
clahe:
  clip_limit: 2.0
  tile_grid_size: [8, 8]

# 边缘损失
edge_loss:
  weight: 0.1
  use_sobel: true

# 纹理损失
texture_loss:
  weight: 0.05
  patch_size: 16
```

---

## 代码实现指南

### 1. 通道适配器实现

```python
# 在模型yaml或代码中添加
class ChannelAdapter(nn.Module):
    def __init__(self):
        super().__init__()
        self.proj = nn.Conv2d(1, 3, kernel_size=1, bias=False)
        # 用ImageNet权重均值初始化
    
    def forward(self, x):
        return self.proj(x)
```

### 2. CLAHE数据增强

```python
# 在数据加载时添加
class CLAHETransform:
    def __call__(self, img):
        img_np = (img.numpy() * 255).astype(np.uint8)
        clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8,8))
        img_clahe = clahe.apply(img_np)
        return torch.from_numpy(img_clahe).float() / 255.0
```

### 3. 组合损失函数

```python
class CombinedLoss(nn.Module):
    def __init__(self):
        super().__init__()
        self.bce = nn.BCELoss()
        self.edge_loss = EdgeAwareLoss(edge_weight=0.1)
        self.texture_loss = TextureAwareLoss(texture_weight=0.05)
    
    def forward(self, pred, target):
        return (self.bce(pred, target) + 
                self.edge_loss(pred, target) + 
                self.texture_loss(pred, target))
```

---

## 总结

### 红外小目标优化的核心思想

```
┌─────────────────────────────────────────────────────────┐
│                                                          │
│   问题: 红外小目标 = 小 + 模糊 + 噪声 + 1通道              │
│                                                          │
│   解决思路:                                               │
│   ┌─────────────────────────────────────────────────┐   │
│   │                                                  │   │
│   │   通道适配 (B)    →  适配1→3通道差异             │   │
│   │        ↓                                        │   │
│   │   CLAHE增强 (C)   →  增强对比度，抑制噪声         │   │
│   │        ↓                                        │   │
│   │   边缘损失 (D)    →  优化边界定位精度             │   │
│   │        ↓                                        │   │
│   │   纹理损失 (E)    →  学习局部亮度模式             │   │
│   │        ↓                                        │   │
│   │   全量组合 (H)    →  全面优化                    │   │
│   │                                                  │   │
│   └─────────────────────────────────────────────────┘   │
│                                                          │
│   预期提升: Baseline ~0.44 → 全量 ~0.56-0.62             │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 实验建议执行顺序

1. **先跑A (Baseline)** - 建立基准
2. **再跑B和C (单独验证)** - 验证单个模块有效性
3. **然后F (B+C组合)** - 验证协同效果
4. **最后G和H (全量)** - 获取最佳性能

---

*文档生成时间: 2026-04-19*
